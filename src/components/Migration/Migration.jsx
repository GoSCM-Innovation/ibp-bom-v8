// Placeholder — commit 2 implements the full migration UI.
export default function Migration({ connection, session }) {
  return (
    <div style={{ padding: 48, textAlign: 'center', color: 'var(--text2)', fontSize: 13 }}>
      Migración de datos maestros — próximamente
    </div>
  )
}
